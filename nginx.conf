server {
  listen 8080;
  server_name _;
  root /usr/share/nginx/html;

  # Serve your exact page at /
  index legal_intake_platform_final.html;

  location / {
    try_files $uri $uri/ /legal_intake_platform_final.html;
  }
}
